= Introduction
:page-topic-type: guide
:page-toclevels: 2
:description: The Couchbase Power BI Connector provides data visualization for the Couchbase platform, using Microsoft's data visualization software.

[abstract]
{description}

== Overview

The Couchbase Power BI Connector integrates Microsoft Power BI with the following Couchbase data sources:

* Couchbase Server
* Capella Operational
* Capella Analytics
* Enterprise Analytics

The connector uses the Couchbase ODBC driver to connect to these data sources.

To use the connector, you must first create an ODBC Data Source Name (DSN) for each data source you want to use in Power BI.
The DSN defines the connection details such as the host, port, authentication method, and SSL settings.
For Couchbase Server and Capella Operational, the ODBC driver connects to the xref:server:analytics:1_intro.adoc[Couchbase Analytics Service], whereas for Capella Analytics and Enterprise Analytics, the driver connects directly to their analytical databases.

After you configure the DSN, you can use the Power BI connector to import data from your Couchbase data source into Power BI.

== Prerequisites

* Ensure that the Analytics Service is running on the target node when using Couchbase Server or Capella Operational as a data source.
* To connect to Couchbase Server, the Power BI connector requires Couchbase Server 7.2.4 or later.

== Install Power BI

Before you start, ensure you have a supported version of Microsoft Power BI Desktop installed.
The Couchbase Power BI Connector requires the Power BI Desktop version March 2017 or later.

If necessary, download Power BI Desktop from the https://www.microsoft.com/en-US/download/details.aspx?id=58494[Microsoft downloads] page. 

NOTE: Power BI is only available for the Microsoft Windows platform;
for other platforms, consider our xref:tableau-connector::index.adoc[Tableau connector].

== Install OpenSSL

OpenSSL is a required dependency for the Couchbase ODBC driver. 

To install OpenSSL: 

. Download OpenSSL 3.x for x64 (recommended version: OpenSSL 3.5.x Light -- the current LTS version)
 from the link:https://slproweb.com/products/Win32OpenSSL.html[Shining Light Productions site].

. Install OpenSSL from the downloaded file.

. Verify that Windows has installed SSL and registered the path of the binary.
Open the command prompt (`cmd`), and enter the following:
+
[source, console]
----
openssl version
----

. If the output indicates OpenSSL is not installed, rather than returning the version that you installed, then you need to add the OpenSSL binary's location to the `PATH` environment:

.. Check the `C:\Program Files` or use `Find` to locate the SSL binary -- usually the path will be something like:
+
[source, console]
----
C:\Program Files\OpenSSL-Win64\bin
----

.. Add this path to the environmental variables list, found in menu:System variables[Path].

.. Now verify that Windows has registered the path of the binary.
Once again, open the command prompt (`cmd`) -- it must be a fresh command prompt, as the earlier one will not have loaded the newly added environmental variables -- and enter the following:
+
[source, console]
----
openssl version
----

== Install the Couchbase ODBC Driver

To install the Couchbase ODBC driver:

. Download the `couchbase-odbc-1.2-win64.msi` file from https://packages.couchbase.com/releases/couchbase-odbc-driver/1.2/couchbase-odbc-1.2-win64.msi[] -- see the xref:release-notes.adoc[release notes page] for more details.

. Double-click the file to open the *Couchbase ODBC Setup Wizard*.

. Install the ODBC drivers from the downloaded file.
This installation provides both ANSI and Unicode drivers.

IMPORTANT: Make sure that you have installed the 64-bit versions of the ODBC drivers.


== Install the Couchbase Power BI Connector

To install the Power BI Connector: 

. Download `couchbase-powerbi-connector-1.0.mez` from https://packages.couchbase.com/releases/couchbase-powerbi-connector/1.0/couchbase-powerbi-connector-1.0.mez[] --
see the xref:release-notes.adoc[release notes page] for more details.

. Follow https://learn.microsoft.com/en-us/power-bi/connect-data/desktop-connector-extensibility#custom-connectors[Microsoft's guide] on configuring a custom connector.

== Verify the Connector Installation

To verify if the Couchbase Power BI Connector is installed correctly:

. Open *Power BI Desktop*.
. Go to menu:Get Data[].
. In the search box, enter `Couchbase Connector`.

If the connector appears in the search results, it is installed correctly.

== Setting up a Secure Connection

Couchbase strongly recommends that you secure your ODBC connection using SSL.
This ensures encryption of the communication between Power BI and Couchbase. 

[tabs]
====
Capella Operational::
+
--
You will need to get a connection string for your database
and generate an API key to plug into the ODBC connector.

[#copy-connection-string]#To obtain the `connection string`:#

. Sign in to Capella as an `Organization Owner` or `Project Owner`.
. Go to menu:Connect[].
. Make a note of the *Public Connection String*.
You'll need this to set up the ODBC connection. 
+
image::get-connection-string.png[copying connection string]
+
. In the next section of the page, you will find the link to create a list of
menu:Allowed IP Addresses[].
Click on the link to create an entry for the IP address of the machine from which you're running Power BI.
+
image::allowed-ip-addresses.png[creating-allowed-ip address]
+
TIP: You can use the btn:[Add Current IP Address] button to fill in the address of the machine currently running the web console.

. [#setup-database-access]#Return to the *Connect* screen and click on the btn:[Cluster Access] link.#
Create a new database access entry, setting values for `bucket`, and `scopes`.
+
image::create-cluster-access.png[Creating cluster access]

. To set up a secure connection, you will need to retrieve the root certificate for your Capella instance.
+
.. Click on btn:[Security Certificate] in the left menu.
.. Click on btn:[Download] to transfer a copy of the root certificate to your local machine.
+
image::download-root-certificate.png[]
--

Capella Analytics::
+
--

For details on creating a Capella Analytics instance,
first see xref:analytics:admin:prepare-project.adoc[Creating a Capella Analytics Cluster].

[#capella-columnar-connection-string]#You will need to access the Capella web console to get the connection string for your analytics database.#

. Sign in to Capella as an `Organization Owner` or `Project Owner`.
. Go to menu:Analytics[].
. Select your Analytics cluster from the list.
. Select menu:Settings[] from the top-level page menu.
+
image::select-columnar-cluster-settings.png[]
+
. Select menu:Connection String[] from the left-hand menu.
+
image::get-capella-columnar-connection-string.png[]
+
. Make a note of the connection string.
. Next, you need to add the IP address of the machine from which you are running Power BI,
 so that Capella will allow the machine to access the columnar data.
. Click on menu:Allowed IP Addresses[] in the left-hand menu.
. Click the btn:[Add Allowed IP] button.
+
image::capella-columnar-add-allowed-ip.png[]
+
. Enter the IP address of the Power BI host machine.
+
TIP: You can use the btn:[Add Current IP Address] button to fill in the address of the machine currently running the web console.

[#capella-columnar-user-account]#Now, you will need to create a user account for Power BI to access the analytics data.#

. Return to the menu:Settings[] page, then click on menu:Access Control[] in the left-hand menu.
. Click on btn:[Create Account].
. Add a `username` and `password` for the new account.
. Make sure you set assign  `sys_view_reader` to the list of roles.
+
image::capella-columnar-user-account.png[]
+
. Click btn:[Create Account] to finish setting up the user account.
--

Couchbase Server::
+
--
[#download-root-certificate]#To create a secure connection to a self-managed Couchbase Server instance, you need to get the root certificate from the Administrator's Console.#

. Sign in to the Couchbase Server Web Console as an Administrator.
. In the left-hand menu, select menu:Security[].
. From the top menu, select menu:Certificates[] to view the security certificates.
+
image::access-root-certificate.png[]
+
. In the *Trusted Root Certificates* section, copy the certificate text starting from `BEGIN CERTIFICATE` to `END CERTIFICATE`.
. Save the text as a .pem file in an accessible location (for example, `C:\Users\user\certificate.pem`).

You will use this file later when you create the ODBC connection.
--

Enterprise Analytics::
+
--
[#download-ea-root-certificate]#To create a secure connection to Enterprise Analytics, you must download the root certificate.#

. Sign in to the Enterprise Analytics Web Console as an Administrator. 
. In the left-hand menu, select menu:Security[].
. From the top menu, select menu:Certificates[] to view the security certificates.
+
image::access-ea-root-certificate.png[]
+
. In the *Trusted Root Certificates* section, copy the certificate text starting from `BEGIN CERTIFICATE` to `END CERTIFICATE`.
. Save the text as a .pem file in an accessible location (for example, `C:\Users\user\certificate.pem`).

You will use this PEM file later when you create the ODBC connection. 
--

====

== Configure an ODBC Data Source 

To use a Couchbase data source in Power BI, you must configure an ODBC Data Source Name (DSN).

During configuration, you can also enable logging for the ODBC driver. 
The driver stores the logs in the following location: <location>.

IMPORTANT: Ensure that use the *ODBC Data Source Administrator (64 bit)*.
The 32-bit version is incompatible with the connector and will not work correctly.

[tabs]
====

Capella Analytics::
+
--
. Open the *ODBC Data Source Administration* tool.
. Go to menu:User DSN[Add].
. Select the *Couchbase ODBC Driver* (either ANSI or Unicode) from the list, and then click btn:[Finish].
. Select *Capella Analytics* as the data source. 
. (Optional) To enable logging, select *Collect Logs*.  
. Click btn:[Next]. 
. Enter the following DSN details:
+
[horizontal]
*Name:*:: A unique name for your data source.
*Description:*:: An optional description.
*Connection String:*:: The connection string copied from <<capella-columnar-connection-string,here>>.
*Database*:: The database from which the data must be extracted.
*Scope:*:: The scope from which the data must be extracted. +
For two-part scopes, use a `/` to separate the two parts. 
For example, if the scope is `travel-sample.inventory`, enter it as `travel-sample/inventory`.
*User*:: The username created in the <<capella-columnar-user-account,User Account section>>.
*Password*:: The password created in the <<capella-columnar-user-account,User Account section>>. +
NOTE: Providing the *Username* and *Password* here is optional.
However, in the Power BI tool, you must provide these credentials when prompted.
. Click btn:[OK] to save the DSN and complete the setup.
--

Capella Operational::
+
--
. Open the *ODBC Data Source Administration* tool.
. Go to menu:User DSN[Add].
. Select the *Couchbase ODBC Driver* (either ANSI or Unicode) from the list, and then click btn:[Finish].
. Select *Capella Operational (Analytics Service)* as the data source. 
. (Optional) To enable logging, select *Collect Logs*.  
. Click btn:[Next]. 
. Enter the following DSN details:
+
[horizontal]
*Name:*:: A unique name for your data source.
*Description:*:: An optional description.
*Connection String:*:: The exact connection string copied from <<capella-columnar-connection-string,here>>.
+
include::partial$common.adoc[tag="remove-prefix-warning"]
*Scope:*:: The scope from which the data must be extracted. +
For two-part scopes, use a `/` to separate the two parts. 
For example, if the scope is `travel-sample.inventory`, enter it as `travel-sample/inventory`.
*User*:: The username created in the <<capella-columnar-user-account,User Account section>>
*Password*:: The password created in the <<capella-columnar-user-account,User Account section>>. +
NOTE: Providing the *Username* and *Password* here is optional.
However, in the Power BI tool, you must provide these credentials when prompted.
. Click btn:[OK] to save the DSN and complete the setup.
--

Couchbase Server::
+
--
. Open the *ODBC Data Source Administration* tool.
. Go to menu:User DSN[Add].
. Select the *Couchbase ODBC Driver* (either ANSI or Unicode) from the list, and then click btn:[Finish].
. On the next window, select *Couchbase Server (Analytics Service)* as the data source. 
. (Optional) To enable logging, select *Collect Logs*.  
. Click btn:[Next]. 
. Enter the following DSN details:
+
[horizontal]
*Name:*:: A unique name for your data source.
*Description:*:: An optional description.
*Address*:: The host and port details.
Enter the IP address of the Data (KV) node in the Couchbase cluster.
If you're using an SSL connection, set the port to `11207`.
*Scope:*:: The scope from which the data must be extracted. +
For two-part scopes, use a `/` to separate the two parts. 
For example, if the scope is `travel-sample.inventory`, enter it as `travel-sample/inventory`.
+
include::partial$common.adoc[tag="extra-spaces-warning"]
*SSL Mode:*:: Select `Enable` or `Disable`. 
*Authentication:*:: Select an authentication method. +
+
Based on the SSL mode and authentication method you select, the required fields differ.
+
[cols="1a,1a,2a,3a", options="header"]
|===
| SSL Mode | Authentication | Fields | Description

| `Disable`
| `Basic`
| Username, Password
| User credentials. 

| `Enable`
| `Basic`
| Username, Password, Cluster Cert Path
| User credentials and the path to the root certificate you downloaded <<download-root-certificate,here>>.

| `Disable`
| `LDAP`
| Username, Password 
| LDAP directory credentials. 

For more information, see xref:server:manage:manage-security/configure-ldap.adoc[Configure LDAP].

| `Enable`
| `LDAP`
| Username, Password, Cluster Cert Path 
| LDAP credentials and the path to the root certificate you downloaded <<download-root-certificate,here>>.

For more information, see xref:server:manage:manage-security/configure-ldap.adoc[Configure LDAP].

| `Enable`
| `Client Certificate`
| Client Cert Path, Client Key Path, Cluster Cert Path
| Paths to your client certificate, private key, and root certificate.

For more information, see xref:server:manage:manage-security/configure-client-certificates.adoc[Configure Client Certificates].

| `Enable`
| `Client Certificate (Encrypted Key)`
| Client Cert Path, Client Key Path, Key Password, Cluster Cert Path 
| Paths to your certificates and encrypted key, including the key passphrase.    

For more information, see xref:server:manage:manage-security/configure-client-certificates.adoc[Configure Client Certificates].
|===
+
NOTE: Providing the *Username* and *Password* in the DSN configuration is optional.
However, in the Power BI tool, you must provide these credentials when prompted.
+
include::partial$common.adoc[tag="windows-separators-warning"]
*Advanced Parameters:*:: Additional parameters for the ODBC connection.
. Click btn:[OK] to save the DSN and complete the setup.
--

Enterprise Analytics::
+
--
. Open the *ODBC Data Source Administration* tool.
. Go to menu:User DSN[Add].
. Select the *Couchbase ODBC Driver* (either ANSI or Unicode) from the list, and then click btn:[Finish].
. On the next window, select *Enterprise Analytics* as the data source. 
. (Optional) To enable logging, select *Collect Logs*.  
. Click btn:[Next]. 
. Enter the following DSN details:
+
[horizontal]
*Name:*:: A unique name for your data source.
*Description:*:: An optional description.
*Address*:: The host and port details.
Enter the IP address of the Data (KV) node in the Couchbase cluster.
If you're using an SSL connection, set the port to `11207`.
*Database*:: The name of the database from which the data must be extracted.
*Scope:*:: The scope from which the data must be extracted. +
For two-part scopes, use a `/` to separate the two parts. 
For example, if the scope is `travel-sample.inventory`, enter it as `travel-sample/inventory`.
*SSL Mode:*:: Select `Enable` or `Disable`. 
*Authentication:*:: Select an authentication method. +
+
Based on the SSL mode and authentication method you select, the required fields differ.
+
[cols="1a,1a,2a,3a", options="header"]
|===
| SSL Mode | Authentication | Fields | Description

| `Disable`
| `Basic`
| Username, Password
| User credentials. 

| `Enable`
| `Basic`
| Username, Password, Cluster Cert Path
| User credentials and the path to the root certificate you downloaded <<download-root-certificate,here>>.

| `Disable`
| `LDAP`
| Username, Password 
| LDAP directory credentials. 

For more information, see xref:enterprise-analytics:manage:manage-security/configure-ldap.adoc[Configure LDAP].

| `Enable`
| `LDAP`
| Username, Password, Cluster Cert Path 
| LDAP credentials and the path to the root certificate you downloaded <<download-root-certificate,here>>.

For more information, see xref:enterprise-analytics:manage:manage-security/configure-ldap.adoc[Configure LDAP].

| `Enable`
| `Client Certificate`
| Client Cert Path, Client Key Path, Cluster Cert Path
| Paths to your client certificate, private key, and root certificate.

For more information, see xref:enterprise-analytics:manage:manage-security/configure-client-certificates.adoc[Configure Client Certificates].

| `Enable`
| `Client Certificate (Encrypted Key)`
| Client Cert Path, Client Key Path, Key Password, Cluster Cert Path 
| Paths to your certificates and encrypted key, including the key passphrase.   

For more information, see xref:enterprise-analytics:manage:manage-security/configure-client-certificates.adoc[Configure Client Certificates].
|===
+
NOTE: Providing the *Username* and *Password* in the DSN configuration is optional.
However, in the Power BI tool, you must provide these credentials when prompted.
+
include::partial$common.adoc[tag="windows-separators-warning"]
*Advanced Parameters:*:: Additional parameters for the ODBC connection.
. Click btn:[OK] to save the DSN and complete the setup.
--
====

== Connect and Add Data to Power BI

Business information tools rely on data organized into relational databases.
To use the Power BI Connector, you must create tabular analytics views of your JSON documents. 

* For Couchbase Server and Capella Operational, see xref:server:analytics:run-query.adoc#Using_analytics_workbench[Analytics Workbench] or 
xref:server:analytics:5a_views.adoc[Tabular Views].

* For Capella Analytics, see xref:analytics:query:views-tavs.adoc[Tabular Views] or xref:analytics/sqlpp/5a_views.html#TAV[Create a Tabular View].

* For Enterprise Analytics, see xref:enterprise-analytics:sqlpp:5a_views.adoc[Tabular Views] or xref:enterprise-analytics:sqlpp:5a_views.adoc#TAV[Create a Tabular View].

After you prepare tabular views and define DSNs, use the Couchbase Power BI Connector to load data into Power BI. 

=== Add Data from Your Data Source

To add data from Couchbase data sources to Power BI: 

. In Power BI Desktop, select menu:Get Data[].
. Select *Couchbase Connector*.
. In the *dsn* field, enter the name of the DSN you created. 
. Select the *Data Connectivity mode* and click btn:[OK]. 
. Select an authentication method based on your ODBC DSN configuration. 
** `Basic` - If your DSN uses `Basic` or `LDAP` authentication, or if you're connecting to Capella Analytics or Capella Operational.
Enter your username and password.
** `Basic` (password only) - If your DSN uses `Client Certificate (Encrypted Key)` authentication.
Enter your password and leave the username field empty.
** `Anonymous` - If your DSN uses `Client Certificate` authentication. 
No certificate details required as this option provides implicit authentication.
** `Windows` - If you want to authenticate with your Windows credentials.
. Click btn:[Connect].

After connecting, Power BI displays a list of the tabular views in the database specified by your DSN. 
Select a view from the list and click btn:[Load] to import it. 
You can then use various Power BI options to visualize the data.

An example image of the `travel-sample` `airport_view`: 

image::visualization.png[A data visualization in Power BI]

== Related Links

* Microsoft documentation
** https://learn.microsoft.com/en-us/power-bi/connect-data/desktop-connect-to-data[Connecting to data sources in Power BI desktop].
** https://support.microsoft.com/en-us/office/administer-odbc-data-sources-b19f856b-5b9b-48c9-8b93-07484bfab5a7#bm2[Adding a data source].
